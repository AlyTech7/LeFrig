# @lefrig/ui

Design system **web-only** para Next.js (apps/web; el admin importa principalmente CSS).

La app móvil Expo **no** consume estos componentes: tiene equivalentes nativos (`PhoneField`, `CurrencySelect`, etc.) por limitaciones de React Native.

No asumir multiplataforma hasta una extracción consciente a primitivos compartidos.
